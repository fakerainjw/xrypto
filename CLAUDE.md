# 프로젝트 컨텍스트

암호화폐 시세 데이터 수집 및 백테스트 프로젝트. 거래소는 업비트(Upbit).

## 개발 환경

- 개발은 주로 Android 스마트폰에서 진행 (GitHub 웹/앱, 원격 Claude Code)
- Termux는 사용하지 않음
- 수집기 실행 환경: GitHub Actions (cron 스케줄)
- 무거운 연산이 필요하면 Codespaces 또는 원격 머신으로 분리
- Python, 의존성 관리는 uv

## 데이터 수집 방침

- 우선순위: 주기적 캔들 갱신 (과거 대량 백필이나 실시간 틱은 후순위)
- 항상 증분 수집: 마지막 저장 시각 이후 구간만 채운다.
  cron이 밀리거나 실행이 스킵돼도 다음 실행에서 자동 복구되어야 함
- 저장 포맷: 월별 parquet (`data/{MARKET}/{UNIT}m/YYYY-MM.parquet`)
- 원본 응답 필드는 최대한 보존 (특히 `candle_acc_trade_price` = 누적 거래대금)

## 업비트 API 주의사항

- 시세 조회(Quotation)는 API 키 불필요
- 마켓 코드는 `KRW-BTC` 형식 (ccxt의 `BTC/KRW`와 다름)
- 캔들은 호출당 최대 200개. `since`가 없고 `to`(마지막 캔들 시각, exclusive) 기준
  역방향 페이지네이션만 가능
- 최근 체결(ticks)은 최대 200건, 호가창은 최대 30호가 레벨
- Origin 헤더를 절대 붙이지 말 것 — 붙으면 시세 조회가 10초당 1회로 제한됨
- Rate limit은 초 단위. 응답의 `Remaining-Req` 헤더에서 `sec` 값만 참조
  (`min`은 deprecated된 고정값)
- 429 → 지수 백오프. 429에도 계속 요청하면 418과 함께 IP/계정 차단되고
  반복 시 차단 시간이 점점 길어짐. 418이면 즉시 중단
- 호출 간격은 초당 10회 이하로 여유 있게 (`time.sleep(0.15)` 수준)
- Base URL이 리전별로 다름: 국내 `api.upbit.com`, 글로벌 `sg-api.upbit.com`.
  GitHub Actions 러너는 해외 IP이므로 시작 시 접근 가능한 엔드포인트를
  판별해 폴백할 것
- 거래가 없는 구간은 캔들이 아예 누락됨. 연속 인덱스를 가정하는 지표는
  reindex로 결측을 명시 처리
- 응답에 `candle_date_time_utc`와 `candle_date_time_kst`가 함께 오고 둘 다
  타임존 표기가 없는 naive 문자열 → 섞이면 9시간이 조용히 어긋남

## 코딩 규칙

- 시각은 내부적으로 항상 UTC, 표시할 때만 KST로 변환
- 가격·금액 계산은 `Decimal` 사용, float 금지
- 미완성 캔들(마감 전 봉)은 저장하지 않음 — 값이 계속 변하므로
  백테스트·시그널 계산을 오염시킴
- 저장 시 `ts` 기준 중복 제거 (페이지 경계에서 중복 발생)
- 심볼 목록은 하드코딩하지 말고 마켓 조회 API로 받아올 것
- API 키가 필요해지면: 조회 전용 권한 + IP 화이트리스트 + 환경변수
  (코드나 저장소에 절대 커밋하지 않음)

## 백테스트 시 반영할 것

- KRW 마켓 수수료 0.05% (왕복 0.1%) — 단타 전략에서 결정적
- 최소 주문 금액 5,000원
- 호가 단위(틱 사이즈)가 가격대별로 다름 → 임의 소수점 가격 체결 가정 금지
- 실거래는 수수료·슬리피지·체결 지연 때문에 백테스트와 상당히 벌어짐

## GitHub Actions 주의사항

- cron은 정시에 안 돎 (수 분~수십 분 지연, 드물게 스킵).
  정시 실행을 전제로 로직을 짜지 말 것
- 정시 혼잡을 피해 `10 * * * *`처럼 어긋난 시각 사용
- `concurrency` 그룹으로 직렬화 (`cancel-in-progress: false`) — 안 하면
  이전 실행과 겹쳐 push 충돌
- 60일간 저장소 활동이 없으면 스케줄이 자동 비활성화됨.
  봇 커밋만으로는 활동 인정이 안 될 수 있으니 주기적으로 확인
- 데이터 커밋에는 `permissions: contents: write` 필요
- 워크플로 실행/로그 확인은 `gh workflow run`, `gh run watch`,
  `gh run view --log`

## 향후 검토

- WebSocket 실시간 수집 (`wss://api.upbit.com/websocket/v1`):
  ticket 필드 필요, 응답이 바이너리 프레임, 유휴 시 서버가 끊으므로 주기적 PING,
  재연결 후 빈 구간은 REST 캔들로 보정. Actions에서는 불가 → 상시 머신 필요
- 다중 거래소 비교가 필요해지면 그때 ccxt로 감싸기
  (지금은 업비트 고유 필드 보존을 위해 직접 `requests` 호출)
